<tr id="account_<%= account.id %>">
  <td><%= link_to(account.no, account_edit_url(:account => account)) %></td>
  <td><%= link_to(account.name, account_edit_url(:account => account)) %></td>
  <td><%= h(account.account_type.name) %></td>
</tr>
